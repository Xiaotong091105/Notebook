# 2026-08-08

## Actions taken today

1. Added the "Previous Requests Revision" doc summarising past clinic project requests.
2. Reviewed PR feedback on how `_index.md` is described in `CLAUDE.md`.
3. Updated `CLAUDE.md` to replace "concept/learning-map" with "Markdown", naming the real selection rule: `_index.md` indexes the curated `.md` file in each folder, not any `.epub`/`.plain.md`/`covers/` siblings alongside it (leftover, gitignored output of a local EPUB-conversion script).
